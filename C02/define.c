#include <stdio.h>
#define WORKER_NUM 12
#define WORK_PLACE 32 //try to add a var with the same name

int main() {
    printf("Worker number: %d is working  in the workplace number %d\n", WORKER_NUM,WORK_PLACE);

    return 0;
}
